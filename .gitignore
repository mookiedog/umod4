# Ignore any directory (and everything under it) that starts with double underscore
__*/
# Ignore any filename starting with double underscore:
__*

build/
*.bak
~$*

# PyInstaller artifacts if built outside build/ directory
tools/logtools/viz/dist/
tools/logtools/viz/build/
tools/logtools/viz/*.spec
